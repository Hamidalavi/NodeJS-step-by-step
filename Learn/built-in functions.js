// random number
// const string = "String";
// const number;

// string.substr(number, number); // cuts the string from beginning and the rest of the length (index of string)

// const generateRandomString = (length = 12) => {
//   Math.random().toString(20).substr(2, length);
// };
